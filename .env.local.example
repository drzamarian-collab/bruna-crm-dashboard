# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
# Production: NEXTAUTH_URL=https://crm.walterzamarianjr.com
NEXTAUTH_SECRET=<generate-with-openssl-rand-base64-32>

# Admin Credentials
ADMIN_EMAIL=drzamarian@gmail.com
ADMIN_PASSWORD_HASH=<generate-with-bcryptjs>

# CRM API Lambda URL
CRM_API_URL=https://LAMBDA-FUNCTION-URL.lambda-url.us-east-1.on.aws

# Instructions:
# 1. Copy this file to .env.local
# 2. Generate NEXTAUTH_SECRET: openssl rand -base64 32
# 3. Generate ADMIN_PASSWORD_HASH: node -e "console.log(require('bcryptjs').hashSync('your-password', 10))"
# 4. Get CRM_API_URL from Lambda bruna-crm-crm-api Function URL
# 5. NEVER commit .env.local to Git (already in .gitignore)
