# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32

# Admin Credentials
ADMIN_EMAIL=drzamarian@gmail.com
ADMIN_PASSWORD_HASH=generate-with-bcrypt

# CRM API Lambda URL
CRM_API_URL=https://lambda-url.execute-api.us-east-1.amazonaws.com/prod

# Instructions:
# 1. Copy this file to .env.local
# 2. Generate NEXTAUTH_SECRET with: openssl rand -base64 32
# 3. Generate ADMIN_PASSWORD_HASH with: node -e "console.log(require('bcryptjs').hashSync('your-password', 10))"
# 4. Replace CRM_API_URL with your actual Lambda Function URL
